import tokensColors from '#panda/tokens/colors'
import tokensSpacing from '#panda/tokens/spacing'
import tokensfontSizes from '#panda/tokens/fontsize'
import tokensFonts from '#panda/tokens/fonts'
import tokensShadows from '#panda/tokens/shadow'

export default {
  base: {
    colors: tokensColors.base,
    fonts: tokensFonts.base,
    fontsizes: tokensfontSizes.base,
    spacing: tokensSpacing.base,
    shadows: tokensShadows.base,
  },
  semantic: {
    colors: tokensColors.semantic,
    fonts: tokensFonts.semantic,
    fontsizes: tokensfontSizes.semantic,
    spacing: tokensSpacing.semantic,
    shadows: tokensShadows.semantic,
  },
}
